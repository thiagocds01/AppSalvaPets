<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base do aplicativo -->
    <style name="Base.Theme.AppSalvaPets" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Cor da barra de status -->
        <item name="android:statusBarColor" tools:targetApi="30">@color/white</item>
        <!-- Ícones escuros na barra de status -->
        <item name="android:windowLightStatusBar" tools:targetApi="30">true</item>

        <item name="colorPrimary">@color/on_green</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        name="CustomButtonStyle" parent="Widget.Material3.Button">

        <item name="android:textColor">@color/black</item>

    </style>

    <!-- Tema aplicado no aplicativo -->
    <style name="Theme.AppSalvaPets" parent="Base.Theme.AppSalvaPets" />
</resources>
